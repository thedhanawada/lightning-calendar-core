{
  "buildCommand": "pwd && ls -la && test -d demo && cd demo && npm install && npm run build",
  "outputDirectory": "demo/dist",
  "installCommand": "echo 'Skipping root install'",
  "framework": null,
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}